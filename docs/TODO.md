# TODO — Centralización de Consentimientos LPDP Interbank (IBK)

**SPEC:** spec-ibk-20260819-001  |  **Status:** draft

### DISCOVERY (sub-etapa de DESIGN)
- [x] Resolver `consentimiento_ibk_archivo` — confirmado: dataset `raw_ibk_dlk`, tabla `t_consent_transaction_{fecha_archivo}_external` (una nueva por fecha, no estática)
- [x] Confirmar project id real de `iden_party` — confirmado `dev-intercorp-data-operation` (guión, no guión bajo; el guión bajo fue rechazado por la API de BigQuery)
- [x] Confirmar qué representa `itc_company_id = '1000'` — confirmado: segundo código de Interbank (misma empresa, 2 códigos)
- [x] Confirmar si `iden_itc_party_prd` tiene la misma estructura que `iden_itc_party` — confirmado por el equipo
- [ ] Generar/cargar glosario propio de `iden_itc_party_prd` (por ahora solo hay glosario de la tabla de referencia `iden_itc_party`, sin acceso BQ para perfilar la `_prd` directamente)
- [ ] Confirmar si `iden_itc_party_prd` también carece de partición (impacto en costo de la SP — la tabla de referencia tiene 467.7M filas sin partición)

### DESIGN
- [x] PHYSICAL_DESIGN completado (2026-08-19) — DDL físico de ambas tablas destino + referencia de tabla externa
  → Ver: data/bigquery/master_party/t_consent_transaction/ddl/, data/bigquery/master_party/ba_customer_consent_group/ddl/
- [x] Cambio de `CREATE OR REPLACE TABLE` → `CREATE TABLE IF NOT EXISTS` en `ba_customer_consent_group.sql` — aceptado por el equipo
- [x] `approval_channel_name` en `ba_customer_consent_group` — confirmado: siempre NULL, no tiene origen en `t_consent_transaction`
- [ ] Confirmar exactitud del offset `folder_date = process_date - 1 día` (RN-IBK-002) con Interbank
- [ ] Definir motor de orquestación (Cloud Workflows / Composer) para los 3 modos de ejecución (normal/manual/reproceso)
- [ ] Confirmar fuentes y volumen con el equipo

### CODING
- [x] SP: `sp_t_consent_transaction_ibk.sql` — `CREATE OR REPLACE EXTERNAL TABLE` dinámico + cruce iden_party (por party_id **e** itc_company_id, ver nota abajo) + DELETE/INSERT por itc_company_id+consent_date + RAISE si el archivo llega vacío
- [x] SP: `sp_ba_customer_consent_group_ibk.sql` — filtro CP_2/otorgado + DELETE/INSERT, acotado al scope que dejó el SP anterior
- [x] Test: `test_sp_t_consent_transaction_ibk.sql` (⚠️ requiere fixture real en GCS para correr — ver notas del archivo) y `test_sp_ba_customer_consent_group_ibk.sql` (ejecutable, sin dependencias externas)
- [x] Confirmar el JOIN de `sp_t_consent_transaction_ibk`: **validado contra datos reales de dev (2026-08-26)** — igualar también `itc_company_id` SÍ generaba un problema real, pero al revés de lo que se temía: excluía ~2.78M filas de personas con doble registro de empresa en `iden_itc_party_prd`, no las duplicaba. Fix: cruce por `party_id` únicamente (RN-IBK-019) — ver esa entrada más abajo
- [x] `sp_t_consent_transaction_ibk` NO elimina `tmp_t_consent_transaction_ibk` al terminar — **ya no aplica** (RN-IBK-019, 2026-08-26): este SP dejó de usar tablas de scope por completo (ahora escribe directo a `t_consent_transaction_raw`); la única tabla temporal que queda en el módulo la crea y elimina `sp_centralizar_t_consent_transaction_ibk.sql`
- [ ] Confirmar el literal `record_source = 'LPDP_IBK'` usado en ambos SPs — no estaba definido en el requerimiento original
- [ ] Implementar encriptación AEAD para `documento_legal_id` y `signed_document` (confirmado por Data Owner) — no incluida en esta pasada de CODING, evaluar si va en los SPs o en una vista posterior

### INTEGRIDAD            ← activada por el usuario (2026-08-19): etapas.integridad: true
- [x] `fuentes[]` completadas con `rol` (principal: `consentimiento_ibk_archivo`, secundaria: `iden_party`), `llave` y `campo_fecha`
- [x] `reglas_integridad` agregado al spec: 1 regla `detener_proceso` (actualidad, D-1) + 4 `excluir_registros` (duplicados/llave_nula en ambas fuentes)
- [x] SP: `sp_integridad_t_consent_transaction.sql` — evalúa actualidad de `consentimiento_ibk_archivo`, sin escribir tablas (solo OUT params)
- [x] Patrón `QUALIFY ROW_NUMBER()` + `WHERE llave IS NOT NULL` aplicado en `sp_t_consent_transaction_ibk.sql` para ambas fuentes
- [x] `deploy_dev.json`/`deploy_prd.json`: SP de integridad agregado en `bigquery_sp`, después del SP de carga principal
- [x] Workflow: gate de integridad insertado dentro del `for` de fechas, antes de cada llamada a `sp_t_consent_transaction_ibk` — si `o_flag_detener=1`, hace `raise` y reutiliza el mecanismo de checkpoint/mail de error ya existente
- [x] `registro_resultados: true` (2026-08-19, tras activar MONITORING) — `registrar_resultado_integridad` (`RegisterIntegrityResults`) agregado al workflow; histórico ahora se matricula en `metadata_operational`
- [x] 5 payloads de matrícula en `data/integrity/master_party/t_consent_transaction/payloads/` (uno por regla), agregados a `deploy_[env].json` bajo `monitoring_register` (después del directorio de monitoring)
- [ ] Confirmar si algún duplicado/llave nula debería escalar a `detener_proceso` en vez de excluirse silenciosamente (hoy los 4 checks secundarios son `excluir_registros`, el default)
- [ ] Validar en TESTING que la tabla externa que crea `sp_integridad_t_consent_transaction.sql` no choque con la que crea `sp_t_consent_transaction_ibk.sql` para la misma fecha (mismo nombre, ambas `CREATE OR REPLACE` — debería ser idempotente pero no se probó en vivo)

### MONITORING            ← activada por el usuario (2026-08-19): etapas.monitoring: true, METADATA_API_URL agregado a env_dev.json
- [x] Bloque `monitoring:` completado en el spec (`METADATA_API_URL` como referencia `${METADATA_API_URL}`, `process.*` con `flag_scheduler`, `tasks[]` con `description`)
- [x] Payloads de matrícula: `data/monitoring/master_party/t_consent_transaction/payloads/{process_t_consent_transaction_ibk, task_sp_t_consent_transaction_ibk, task_sp_ba_customer_consent_group_ibk}.json`
- [x] `deploy_dev.json`/`deploy_prd.json`: clave `monitoring_register` agregada (payloads de monitoring + integridad)
- [x] `env_prd.json`: `METADATA_API_URL` completado con el valor conocido del checklist del estándar
- [x] Workflow: variables monitoring en `set_vars`, `var_execution_type` derivado de `var_modo` (NORMAL/MANUAL/REPROCESO — no fijo como sugiere la plantilla genérica), ambos SPs instrumentados con `TrackedBigQueryJobWithResults`, `UpdateProcessStatus` al final del `try` y en el `except`, `MainResponse` extendido con métricas
- [x] Sub-workflows agregados: `TrackedBigQueryJobWithResults`, `CreateExecution`, `UpdateExecution`, `UpdateProcessStatus`, `RegisterIntegrityResults` (`SyncBigQueryJobWithResults` ya existía desde INTEGRIDAD)
- [x] Ambos SPs (`sp_t_consent_transaction_ibk.sql`, `sp_ba_customer_consent_group_ibk.sql`): 2 parámetros `OUT` (`o_execution_data_read`, `o_execution_data_write`) agregados al final de la firma, capturados con `@@row_count` (write) y `COUNT(1)` sobre la tabla de stage (read)
- [x] Tests actualizados con los 2 argumentos `OUT` nuevos + asserts de las métricas
- [ ] ⚠️ Prerequisito IAM manual: la SA de Cloud Build (`trv-itcbi-devops-app@itc-data-devops-01.iam.gserviceaccount.com`) necesita `roles/run.invoker` sobre la metadata API (`METADATA_API_URL`) para que la matrícula funcione en deploy
- [ ] Confirmar `company_id`/`company_code` (`"000"`/`"IBK"`) en el catálogo de control de procesos — se asumieron iguales a `c_itc_company`, pero es un catálogo distinto
- [ ] Confirmar el nombre real del workflow desplegado en GCP coincide con `${env}-t-consent-transaction-ibk` (usado en `monitoring.process.code` y en los payloads)

### DATA QUALITY          ← activada por el usuario (2026-08-19): etapas.data_quality: true
- [x] SP DQ: `sp_dq_t_consent_transaction.sql` (3 reglas), `sp_dq_ba_customer_consent_group.sql` (2 reglas) — iteran `dq_config`, registran en `dq_control`
- [x] `dml_dq_config_t_consent_transaction.sql` / `dml_dq_config_ba_customer_consent_group.sql` — INSERT de las 5 reglas, agregados a `bigquery_dml` (solo `deploy_dev.json`, per regla del framework "DML solo corre en dev")
- [x] Workflow: `ejecutar_dq_tct`/`ejecutar_dq_bccg` agregados después de cada SP de carga (dentro del `for` de fechas); chequeo agregado `validar_dq_criticas` después del loop, no detiene el pipeline — solo marca el asunto del mail como "[⚠️ OK CON DQ FAILS]" si hay reglas críticas en fail
- [ ] ⚠️ Mapeo de vocabulario asumido: spec usa `dimension: unicidad|validez`, el framework central (`data-quality.md`) usa `dq_dimension: duplicidad|conformidad` — mapeé `unicidad→duplicidad` y `validez→conformidad`. Confirmar que es el mapeo correcto con el equipo dueño de `itcm-dp-dataquality-core`
- [ ] ⚠️ No se generó `bigquery_dml` en `deploy_prd.json` (regla del framework: DML solo en dev) — esto deja sin resolver cómo se registran las reglas DQ en producción; confirmar con el equipo el mecanismo real
- [ ] ⚠️ `validar_dq_criticas` cuenta fails por `execution_date = CURRENT_DATE()` sin distinguir `execution_id` — si se corre más de una vez el mismo día (normal + reproceso manual), el conteo mezcla ambas corridas
- [ ] No se generaron `dml_dq_monitor_config_*.sql` — el spec no declara monitores DQ explícitos, solo reglas
- [x] **BUG (2026-08-20):** trigger Cloud Build falló en `deploy-dml` — `Not found: Table dev-itc-customer-services:demo_migracion.dq_config was not found`. Nunca se creó el DDL de `dq_config`/`dq_control`, solo el `INSERT` (`dml_dq_config_*`). Fix: agregados `ddl_dq_config.sql`/`ddl_dq_control.sql` en `t_consent_transaction/ddl/` (`CREATE TABLE IF NOT EXISTS`, tablas compartidas del `dataset_dq`), registrados en `componentes[]` del spec y al inicio de `bigquery_ddl` en `deploy_dev.json`/`deploy_prd.json`. Pendiente: re-disparar el trigger Dataops dev

### COMPLIANCE
- [x] `fac-data-rules-check` ejecutado (2026-08-19) — 2 críticas + 2 advertencias encontradas y corregidas. Reporte: `docs/reports/rules-check-2026-08-19.md`
- [x] Campos DQ (`dq_flag_ind`, `dq_control_msg`, `dq_config_id`) agregados vía `alter/` a ambas tablas destino — quedan `NULL`, el modelo DQ actual valida a nivel de tabla, no de fila
- [x] Nombres de tabla temporal hardcodeados (`tmp_t_consent_transaction_ibk`, `tmp_ba_customer_consent_group_ibk`) — documentados como excepción aceptada en `restricciones[]`, no se crearon variables Dataops nuevas
- [x] Verificar campos de auditoría (load_date, record_source, creation_user) — presentes en ambos DDL
- [x] `fac-data-stage-compliance` ejecutado (2026-08-19) contra los 5 dominios completos de `data/rules/` — 1 ALTA, 3 MEDIA, 2 BAJA. Reporte: `docs/reports/compliance-2026-08-19.md`
- [x] Hallazgo ALTA corregido: `sp_t_consent_transaction_ibk.sql` y `sp_integridad_t_consent_transaction.sql` ahora reciben `p_process_date_ini`/`p_process_date_end` (mismo valor en ambos) en vez de un único `p_process_date` — actualizado también el workflow (`build_sql_integridad_p1`, `build_sql_tct_p1`) y el test `test_sp_t_consent_transaction_ibk.sql`
- [ ] ⚠️ 3 hallazgos MEDIA aceptados como excepción documentada (no corregidos — son contradicciones entre documentos del propio framework, no bugs del módulo): cabecera ausente en el workflow (`data/rules/workflow.md` vs `data/standard/services/workflow.md`), `SELECT *`/`t.*` en el patrón de dedup de `sp_t_consent_transaction_ibk.sql` (`data/rules/bigquery.md` vs `data/standard/data-integrity.md` §4), `DROP TABLE IF EXISTS` sobre las 2 tablas tmp efímeras en `sp_ba_customer_consent_group_ibk.sql`
- [ ] 2 hallazgos BAJA sin corregir (cosméticos): falta `deploy_qa.json`/`env_qa.json` en el repo; naming de `wf-`/`cs-` no sigue literalmente `{tabla_out_kebab}-{emp}`
- [x] **BUG real confirmado (2026-08-20):** trigger Cloud Build falló en `deploy-workflow` (`service_account` inválido/vacío) — el hallazgo MEDIA de cabecera ausente en `wf-ibk-consentimiento.yaml` sí rompía el deploy. `data/standard/services/workflow.md` ("sin cabecera") está desactualizado; la guía correcta es `dataops-configurator/SKILL.md` §8. Fix: agregada cabecera `name: ${env}-t-consent-transaction-ibk` / `region` / `project` / `service_account: ${service_account_job}` antes de `source:`. `name` coincide con el que ya usaba `cs-ibk-consentimiento.yaml`, sin cambios ahí
- [x] **BUG real confirmado (2026-08-20):** `gcloud workflows deploy` falló con `parse error: maximum length for an expression is 400 characters` en el step `handle_error` (408 bytes). Auditados TODOS los `${...}` del workflow (script con plegado YAML + conteo en bytes UTF-8, no caracteres) — solo esa expresión superaba el límite; `build_sql_tct_p1` quedaba a 26 bytes del límite tras agregar `p_process_date_ini/end`. Fix: `handle_error` partido en `build_content_error_p1`/`p2` (con sus `next:` corregidos en `sugerencia_reproceso`/`sugerencia_simple`, que antes saltaban directo a `handle_error` saltándose el build); `build_sql_tct` repartido de 2 a 3 partes. Máximo verificado tras el fix: 324 bytes en todo el archivo
- [x] **BUG de framework confirmado (2026-08-20):** `metadata_register_build` falló con HTTP 422 al matricular `RI-IBK-T_CONSENT_TRANSACTION-001` — la API de metadata NO tiene la tabla `ct_datapipeline_integrity_rule` documentada en `data/standard/data-integrity.md` §6.3; matricula todo (SPs y reglas de integridad) como `ct_datapipeline_task` (mismo endpoint/schema que los 2 SPs de carga que sí pasaron). El 422 devolvió los campos exactos requeridos (`object_catalog/schema/name/type`, `flag_reprocess`) y prohibidos (`task_code`, `source_id`, `check_type`, `tolerance_days`, `key_columns`, `date_field`, etc.). Fix: reescritos los 5 `integrity_rule_t_consent_transaction_ibk_00{1..5}.json` con la forma real de task — `object_catalog/schema/name` apuntando a la fuente validada (`consentimiento_ibk_archivo` para 001-003, `iden_party` para 004-005), `technical_name` = SP que implementa la regla, `code` = el ID de regla (único por archivo)
- [x] **Fix confirmado por el usuario (2026-08-21):** `tmp_t_consent_transaction_ibk` se creaba bajo `${project_t_consent_transaction}` (dev-intercorp-data-storage) pero el dataset de stage vive físicamente bajo `dev-intercorp-data-operation`. Corregido en `sp_t_consent_transaction_ibk.sql` (`v_stage_path`) y `sp_ba_customer_consent_group_ibk.sql` (`v_scope_path`, debe apuntar a la misma tabla física) usando `${project_iden_party}` — resuelto por Dataops en deploy, sin agregar parámetros nuevos a los SPs ni tocar el Workflow. Actualizados también los fixtures/asserts de `test_sp_ba_customer_consent_group_ibk.sql` (creación del scope y el chequeo T5 de limpieza, que ahora valida cada tmp table en su propio proyecto)
- [x] **BUG real confirmado (2026-08-21), reaparición del mismo error:** `sp_ba_customer_consent_group_ibk` falló con `Dataset dev-intercorp-data-storage:demo_migracion was not found`. El fix anterior solo corrigió `v_scope_path` (la tabla que genera `sp_t_consent_transaction_ibk`), pero `v_filtered_path` (la tabla scratch PROPIA de esta SP, `tmp_ba_customer_consent_group_ibk`) seguía usando `p_project_output` en vez de `${project_iden_party}` — mismo bug, no lo había cubierto porque el pedido original solo mencionó la primera tabla. Corregido; también el assert T5 del test (seguía chequeando `tmp_ba_customer_consent_group_ibk` bajo `project_ba_customer_consent_group`)
- [x] **BUG real confirmado (2026-08-21):** `sp_t_consent_transaction_ibk` falló en el INSERT — `Query column 18 has type TIMESTAMP which cannot be inserted into column load_date, which has type DATETIME`. La tabla real en `dev-intercorp-data-storage.master_party.t_consent_transaction` **ya existía con `load_date DATETIME`** antes de este deploy — `CREATE TABLE IF NOT EXISTS` nunca la recreó, y el contrato original (`input/ddl_output_t_consent_transaction.txt`) decía `TIMESTAMP`, que es lo que implementamos fielmente. Fix: `sp_t_consent_transaction_ibk.sql` ahora inserta `CURRENT_DATETIME('America/Lima')` (igual que ya hacía `sp_ba_customer_consent_group_ibk.sql`, que no tuvo este problema porque su tabla si se creó fresca con el tipo del DDL). DDL `t_consent_transaction.sql` actualizado a `DATETIME` con comentario documentando la discrepancia contrato-vs-realidad. **No se tocó la tabla real** (cambiar TIMESTAMP↔DATETIME requiere recrearla — no se hace sin confirmación explícita)
- [x] **HALLAZGO DE NEGOCIO confirmado con Interbank (2026-08-21):** primera corrida con archivo real de dev — "no se cargó nada" en `t_consent_transaction`, sin error. Diagnóstico: descargado el `.txt.gz` real (6,822,318 filas), `consent_transaction_id` viene **vacío en el 100% de las filas** (confirmado también para el archivo real de producción, no solo el de prueba). La regla RI-IBK-T_CONSENT_TRANSACTION-003 (`WHERE ... consent_transaction_id IS NOT NULL`) excluía absolutamente todas las filas antes del INSERT — la tabla externa sí tenía datos (no disparaba el RAISE de "0 filas"), pero el staging quedaba vacío. **Decisión del usuario:** la nueva llave de evento único es `party_id + consent_date_time` (no `consent_transaction_id`). Cambios:
  - `sp_t_consent_transaction_ibk.sql`: `PARTITION BY party_id, consent_date_time` (antes `party_id, consent_transaction_id`), `WHERE party_id IS NOT NULL AND consent_date_time IS NOT NULL`, tie-break por `load_date DESC` (antes ordenaba por el propio `consent_date_time`, que ahora es parte de la partición)
  - `spec.yaml`: `fuentes.consentimiento_ibk_archivo.llave` y `campos_join_clave` → `[party_id, consent_date_time]`; `volumetria` corregida de 60000 (estimado) a 6822318 (real); 2 entradas nuevas en `restricciones[]`
  - `dml_dq_config_t_consent_transaction.sql`: regla DQ-IBK-T_CONSENT_TRANSACTION-003 reescrita a duplicados de `customer_id + consent_date` (aproximación disponible en la tabla final — la llave real de carga, con `consent_date_time`, no se persiste como columna)
  - `conset_transaction_id` se sigue seleccionando/guardando (columna de auditoría), ya no participa en ninguna regla — documentado en el DDL
  - ⚠️ **Pendiente antes de redesplegar `dml_dq_config_t_consent_transaction.sql`:** este componente hace `INSERT` plano (no upsert) — si se reincluye en `bigquery_dml` sin antes borrar la fila vieja `DQ-IBK-T_CONSENT_TRANSACTION-003` en `dq_config`, queda **duplicada** (la vieja regla sobre `conset_transaction_id` seguiría activa junto a la nueva). `deploy_dev.json` sigue restringido a solo `bigquery_sp`, así que no hay riesgo inmediato
- [x] **BUG CRÍTICO confirmado (2026-08-21):** `sp_ba_customer_consent_group_ibk` insertó ~6.7 **mil millones** de filas (esperadas ~3.7M) — reportado por el usuario comparando contra el conteo de `t_consent_transaction`. Causa: el filtro de negocio hacía `INNER JOIN` entre `t_consent_transaction` y `scope` (`tmp_t_consent_transaction_ibk`) por `itc_company_id + consent_date` — pero `scope` tiene **una fila por evento** (no una fila por company+date), así que el JOIN multiplicaba cada fila de `t_consent_transaction` por cada fila de `scope` que compartiera la misma fecha (fan-out clásico). El test unitario no lo detectó porque su mock de `scope` usa `SELECT DISTINCT` (ya deduplicado por company+date), que no reproduce el caso real de múltiples eventos por fecha. Fix: `INNER JOIN` reemplazado por `EXISTS` (semi-join) — acota el escaneo sin multiplicar filas.
  - **Autocorrección sin DELETE manual:** el propio `DELETE FROM ba_customer_consent_group WHERE EXISTS (... scope = filtered_path recién calculado ...)` que ya hace la SP (paso previo al INSERT) barre TODAS las filas existentes para cada `(itc_company_id, consent_date)` presente en el nuevo `filtered_path` corregido — incluidas las miles de millones de filas duplicadas de la corrida con bug, porque comparten las mismas fechas. **No hace falta un DELETE manual de limpieza**: basta con desplegar el fix y volver a correr el workflow para la misma fecha; el propio DELETE+INSERT se autocorrige. (Se había planteado inicialmente un DELETE quirúrgico manual — ya no es necesario)
- [x] **DISEÑO confirmado con el usuario (2026-08-24), RN-IBK-010:** en producción SÍ habrá huecos reales sin archivo (fines de semana). Antes, cualquier fecha sin archivo (`flag_detener=1` del gate de integridad) abortaba TODO el proceso — en un reproceso multi-fecha, la primera fecha con hueco detenía el resto sin llegar a fechas posteriores que sí tenían archivo (confirmado con un ejemplo real: rango 17-21 ago con huecos en 16/18/20). Fix:
  - `wf-ibk-consentimiento.yaml`: `evaluar_integridad` ya no hace `raise` — salta a `registrar_fecha_sin_archivo` (loguea INFO, acumula en `var_fechas_sin_archivo`) y converge en `fin_iteracion_fecha` (mismo punto que el camino exitoso vía `log_fin_fecha`), pasando a la siguiente fecha del rango en vez de abortar. El correo de éxito ahora incluye cuántas fechas se omitieron y cuáles. El histórico de integridad (FAILED, sin datos) se sigue matriculando igual que antes vía `registrar_resultado_integridad`, que corre ANTES de esta decisión
  - `cs-ibk-consentimiento.yaml`: cron cambiado de `0 10 * * *` a `0 10 * * 1-5` (lunes-viernes) para no disparar corridas innecesarias en fin de semana
  - Nueva regla `RN-IBK-010` documentada en spec.yaml
  - Verificado: ninguna expresión del workflow supera 400 bytes tras el cambio (máximo 324)
- [x] **CAMBIO DE DISEÑO confirmado con el usuario (2026-08-25):** `ba_customer_consent_group` ahora refleja solo el ÚLTIMO evento por cliente (`id`), no el histórico completo de eventos 'otorgado' — alineado con el patrón ya establecido en las SPs hermanas de Interseguro/Interfondos (`sp_carga_ba_customer_consent_group`, que ya escriben a la misma `t_consent_transaction`). Implementado en `sp_ba_customer_consent_group_ibk.sql` con `QUALIFY ROW_NUMBER() OVER (PARTITION BY id ORDER BY consent_date DESC) = 1 AND consent_type = 'otorgado'`. **Detalle crítico corregido durante la implementación:** el `consent_type = 'otorgado'` debe ir en el `QUALIFY`, no en el `WHERE` — si se filtra antes de rankear, un cliente cuyo evento más reciente es 'rechazado' seguiría apareciendo con un 'otorgado' más viejo (justo lo que el cambio busca evitar). Se usa `consent_date` (precisión de día, sin hora) como criterio de orden — el usuario decidió explícitamente NO agregar `consent_date_time` como columna nueva en `t_consent_transaction` solo para desempatar. Agregados tests T6 (último evento gana) y T7 (caso crítico: otorgado viejo + rechazado reciente → cliente excluido por completo) en `test_sp_ba_customer_consent_group_ibk.sql`; T0/T2 actualizados de 2 a 3 filas esperadas. `spec.yaml`: RN-IBK-006 actualizada, nueva entrada en `restricciones[]`
- [x] **CAMBIO DE DISEÑO confirmado con el usuario (2026-08-26), reemplaza el del 2026-08-25:** la regla "último evento gana" se movió de `ba_customer_consent_group_ibk.sql` a `sp_t_consent_transaction_ibk.sql` — ahora `t_consent_transaction` misma guarda solo el último evento por cliente (`id`), no el historial completo (contradice la definición original del contrato, aceptado explícitamente). Nueva regla `RN-IBK-011`. Cambios:
  - `sp_t_consent_transaction_ibk.sql`: `QUALIFY ROW_NUMBER() OVER (PARTITION BY p.id ORDER BY a.consent_date DESC) = 1` al final de la query que arma `v_stage_path`, después del `INNER JOIN` con iden_party — rankea TODOS los eventos (otorgado y rechazado), sin filtrar por consent_type
  - `sp_ba_customer_consent_group_ibk.sql`: **revertido** el `ROW_NUMBER`/`QUALIFY` que se le había agregado ayer — ya no hace falta, solo filtra `CP_2`/`otorgado` sobre una fuente que llega ya deduplicada
  - `t_consent_transaction.sql` (DDL): descripción actualizada (ya no dice "cada evento")
  - `test_sp_t_consent_transaction_ibk.sql`: nuevo T5 (no debe haber >1 fila por `id`)
  - `test_sp_ba_customer_consent_group_ibk.sql`: revertidos T6/T7 y el fixture PARTY-005/006 de ayer (ya no son escenarios de entrada válidos para esta SP); T0/T2 vuelven a esperar 2 filas
  - `dml_dq_config_t_consent_transaction.sql` (DQ-003) y `dml_dq_config_ba_customer_consent_group.sql` (DQ-002): reescritas a "sin duplicados de `id`" (antes checkeaban por `customer_id`/`id` + `itc_company_id` + `consent_date`, ya no tan preciso con el nuevo grano). **Ninguno de los dos está en `bigquery_dml` del deploy actual** (sigue excluido por el pendiente de limpieza de `dq_config` ya anotado arriba)
  - `spec.yaml`: RN-IBK-006 actualizada, nueva RN-IBK-011, componentes[] y restricciones[] actualizados
- [x] **BUG real confirmado por el usuario (2026-08-26):** reprocesar un rango acotado (ej. 2026-08-13 a 2026-08-17) seguía insertando `consent_date` de 1900 en `t_consent_transaction`. Causa: `p_process_date_ini`/`p_process_date_end` **nunca filtraban los datos** — solo determinaban qué carpeta GCS leer (`folder_date = p_process_date_ini - 1`). El archivo completo (con TODO su historial, incluidos valores centinela de 1900) se insertaba sin importar el rango solicitado; `p_process_date_end` era un parámetro muerto (aceptado pero nunca referenciado en el cuerpo del SP). Fix (RN-IBK-012): agregado filtro por rango en `sp_t_consent_transaction_ibk.sql`, antes del `QUALIFY` de RN-IBK-011 — mismo patrón que ya usan Interseguro/Interfondos.
  - **BUG de implementación descubierto al desplegar (2026-08-26):** el primer intento referenciaba `p_process_date_ini`/`p_process_date_end` **directamente por nombre dentro del SQL dinámico** (`EXECUTE IMMEDIATE`) — error real: `Unrecognized name: p_process_date_ini`. Los parámetros del SP no son visibles dentro del texto SQL que arma `EXECUTE IMMEDIATE`; hay que concatenar su valor como literal, igual que `v_folder_date`/`v_ext_table_path` en el resto del archivo. Fix inicial: nuevas variables `v_date_ini_lit`/`v_date_end_lit` (vía `FORMAT_DATE('%F', ...)`) concatenadas con `||`, usando `DATE("...")` (comillas dobles) para evitar anidar comillas simples dentro del bloque `'''..'''` que arma toda la query
  - **BUG DE DISEÑO real, detectado por el usuario antes de volver a correr (2026-08-26), 2ª iteración de RN-IBK-012:** filtrar contra `p_process_date_ini/end` (calculado desde `folder_date`) era la comparación equivocada — se verificó con los 3 archivos reales de dev que el desfase entre `folder_date` y el `consent_date` real del contenido **no es consistente ni siquiera entre archivos de la misma semana** (`20260817`: desfase -1 día; `20260819` y `20260821`: desfase 0). No existe ninguna fecha calculada externamente que sea confiable. **Fix definitivo (propuesto por el usuario):** calcular `v_max_consent_date = SAFE_CAST(MAX(consent_date) AS DATE)` directamente de la tabla externa (antes del cruce con iden_party) y filtrar `consent_date = v_max_consent_date` — se adapta automáticamente al desfase real de cada archivo sin asumir nada, y de paso excluye la basura de 1900 sin necesitar ningún parámetro (un centinela nunca va a ser el máximo real). `p_process_date_ini/end` ya no participan del filtro de datos — solo determinan `folder_date` (RN-IBK-002) y el sufijo de tablas temporales (RN-IBK-013)
  - El Workflow sigue llamando una vez por día con `ini=end`; cada llamada ahora se autoajusta a la fecha real más reciente de SU PROPIO archivo, en vez de asumir una relación fija con `process_date`
- [x] **Mejora pedida por el usuario (2026-08-26), RN-IBK-013 — colisión entre ejecuciones concurrentes:** las tablas temporales de scope (`tmp_t_consent_transaction_ibk`, `tmp_ba_customer_consent_group_ibk`) tenían nombre fijo, compartido por CUALQUIER fecha/ejecución del Workflow — si el Cloud Scheduler dispara el modo normal mientras corre un reproceso manual, ambas ejecuciones se pisarían la misma tabla física. Fix: ambos nombres ahora llevan sufijo `_{YYYYMMDD}` (fecha del proceso, `p_process_date_ini`).
  - `sp_t_consent_transaction_ibk.sql`: nueva variable `v_process_date_lit`, usada en `v_stage_path`
  - `sp_ba_customer_consent_group_ibk.sql`: **agregó 2 parámetros nuevos** `p_process_date_ini`/`p_process_date_end` (antes esta SP no recibía fecha alguna) — solo para el sufijo, no filtra datos por fecha. Deben coincidir con lo que usó la llamada a `sp_t_consent_transaction_ibk` para la misma fecha del Workflow
  - `wf-ibk-consentimiento.yaml`: `build_sql_bccg` repartido de 2 a 3 partes (el CALL pasó a 11 parámetros), pasa `fecha_actual` dos veces al inicio del CALL
  - Tests actualizados (`v_fecha_prueba` nuevo en el test de bccg, nombres de tabla temporal con sufijo `_20260401` en el fixture y en T5)
  - `spec.yaml`: nueva RN-IBK-013, componentes[] y restricciones[] actualizados (la excepción de REGLA 2/3 ahora es solo sobre el PREFIJO, no sobre el nombre completo)
- [x] **3 mejoras pedidas por el usuario (2026-08-26), implementadas juntas:**
  1. **`record_source` con nombre de archivo (RN-IBK-016):** `t_consent_transaction.record_source` pasa de literal fijo `'LPDP_IBK'` a `'LPDP_IBK_' || {nombre del archivo}`, usando el pseudo-campo `_FILE_NAME` de BigQuery (no cubierto por `t.*`, seleccionado explícito) recortado a solo el nombre (`REGEXP_EXTRACT(..., r'[^/]+$')`). Solo `t_consent_transaction` — `ba_customer_consent_group` no cambia
  2. **Carga histórica en reproceso (RN-IBK-015):** nuevo parámetro `p_carga_historica BOOL` en `sp_t_consent_transaction_ibk.sql`. El Workflow, SOLO en modo reproceso, chequea `SELECT COUNT(1) FROM t_consent_transaction WHERE itc_company_id IN ('000','1000')` antes del loop de fechas — si es 0, el primer archivo del rango (`process_date_init`) se carga sin el filtro de `MAX(consent_date)` (trae todo su historial); el resto de fechas del mismo rango sigue el comportamiento normal. RN-IBK-011 (último evento por cliente) sigue aplicando siempre, incluso en carga histórica
  3. **Offset -1 solo en modo normal (RN-IBK-014):** el offset `folder_date = process_date - 1` se movió del SP al Workflow — ahora solo se aplica en el step `modo_normal` (fecha de sistema). En manual/reproceso, la fecha que manda el usuario apunta DIRECTO a la carpeta, sin restarle un día. `sp_t_consent_transaction_ibk.sql` y `sp_integridad_t_consent_transaction.sql` ya no restan nada — usan `p_process_date_ini` tal cual
  - Verificado: YAML del workflow parsea OK, ninguna expresión supera 400 bytes (máx 324), sin patrón `": "` prohibido, comillas triples balanceadas en ambos SPs
  - Tests actualizados: `test_sp_t_consent_transaction_ibk.sql` — `v_fecha_prueba` ahora representa la carpeta directamente (antes asumía offset -1), ambos `CALL` con el nuevo parámetro `p_carga_historica = false`, nota T6 documentando cómo probar la carga histórica manualmente
  - `spec.yaml`: RN-IBK-002 actualizada, nuevas RN-IBK-014/015/016, componentes[] y restricciones[] actualizados
- [x] **Investigación de performance pedida por el usuario (2026-08-26):** ejecución de reproceso (07→17 ago, 11 fechas) tardó 1h03m (`aee54ecb-f2f7-440e-b08a-421a67e47b61`). Timeline reconstruido desde Cloud Logging (`logName=".../logs/Workflows"` + `resource.labels.workflow_id` + rango de `timestamp` — combinar ese `logName` con `labels.execution_id` devolvía páginas vacías con `nextPageToken`, sin causa raíz confirmada, se evitó el filtro). Hallazgo: los SP de negocio (`sp_t_consent_transaction_ibk` ~1:30-2:05, `sp_ba_customer_consent_group_ibk` ~0:30-0:35) NO son el cuello de botella — lo son los dos SP de **Data Quality** (`sp_dq_t_consent_transaction` ~2:40-3:15, `sp_dq_ba_customer_consent_group` ~2:10-2:45), que hacen `COUNT(*)` de tabla completa (2 por regla activa: 3 reglas en tct, 2 en bccg) de forma secuencial, sin filtrar por fecha. Suman 60-70% del tiempo por fecha. Contribuye también un piso fijo: `BigQueryJobState` (`wf-ibk-consentimiento.yaml`) hace `sys.sleep(30)` ANTES del primer chequeo de estado del job, en cada llamada a BigQuery. No se tocó el código de DQ en esta pasada — solo se reportó el hallazgo.
- [x] **Mejora pedida por el usuario (2026-08-26), RN-IBK-017 — ba_customer_consent_group pasa a espejo completo:** `sp_ba_customer_consent_group_ibk.sql` dejó de acotar por fecha/scope (ya no lee `tmp_t_consent_transaction_ibk_{fecha}`) — ahora es `TRUNCATE TABLE` + `INSERT` de TODO lo que haya en `t_consent_transaction` que cumpla `conset_id = 'CP_2' AND consent_type = 'otorgado'`. `p_process_date_ini/end` y `p_dataset_stage` quedan en la firma sin usarse para filtrar (decisión explícita del usuario: mantener el parámetro aunque no se use).
  - `wf-ibk-consentimiento.yaml`: los pasos de SP2 (`build_sql_bccg_*`, `invoque_sp_bccg`, `build_sql_dq_bccg`, `ejecutar_dq_bccg`) se sacaron de dentro de `iterar_fechas` — ya NO se llaman una vez por fecha, se llaman UNA sola vez, después de que termina el loop de fechas (evita truncar/reconstruir la tabla completa N veces en el mismo reproceso). Se les pasa `var_process_dates[0]`/`var_process_dates[len(...)-1]` en vez de `fecha_actual`, solo para trazabilidad en monitoring
  - `sp_t_consent_transaction_ibk.sql`: ahora elimina su propia tabla de stage (`tmp_t_consent_transaction_ibk_{fecha}`) al finalizar (punto 7) — antes la eliminaba `sp_ba_customer_consent_group_ibk`, que dejó de tocarla por completo
  - Tests actualizados: `test_sp_ba_customer_consent_group_ibk.sql` (nuevo fixture PARTY-005 con `consent_date` de 2020 para probar que ya no hay filtro de fecha — T0/T2 pasan a esperar 3 filas, T5 reescrito), `test_sp_t_consent_transaction_ibk.sql` (nuevo T7: la tabla de stage debe quedar eliminada)
  - `spec.yaml`: nueva RN-IBK-017, RN-IBK-006/componentes[]/restricciones[] actualizados
  - Verificado: YAML del workflow parsea OK, ninguna expresión supera 400 bytes (máx 324), sin patrón `": "` prohibido, comillas triples balanceadas en ambos SPs
- [x] **Mejora pedida por el usuario (2026-08-26), RN-IBK-018 — se quitan del Workflow los gates de integridad y calidad de datos:** "que se enfoque en procesar e insertar, o bueno, registrar en el API". Eliminados de `wf-ibk-consentimiento.yaml`: el gate de integridad por fecha (`build_sql_integridad_*`, `ejecutar_validacion_integridad`, `registrar_resultado_integridad` vía `RegisterIntegrityResults`), las 2 llamadas a SP de DQ (`sp_dq_t_consent_transaction`, `sp_dq_ba_customer_consent_group`) y la validación post-loop de reglas DQ críticas (`build_sql_validar_dq`/`validar_dq_criticas`, con su resumen en el mail de éxito). Subworkflows sin uso tras el cambio (también eliminados): `RegisterIntegrityResults`, `SyncBigQueryJob` (sin resultados). Los archivos `sp_integridad_t_consent_transaction.sql`/`sp_dq_t_consent_transaction.sql`/`sp_dq_ba_customer_consent_group.sql` y las tablas `dq_config`/`dq_control` NO se borraron del repo, solo dejaron de invocarse — **pendiente decidir** si también se sacan de `deploy_dev.json` (`bigquery_sp`) o se dejan desplegadas sin uso.
  - **Detección de "fecha sin archivo" (RN-IBK-010) se mantiene, cambia el mecanismo:** antes dependía del SP de integridad (evaluaba actualidad ANTES de llamar a `sp_t_consent_transaction_ibk`). Ahora se reutiliza el `RAISE` que ya lanzaba `sp_t_consent_transaction_ibk.sql` cuando la tabla externa tiene 0 filas (su punto 4) — el Workflow envuelve `invoque_sp_tct` en su propio try/except, reconoce ese mensaje puntual (`"no se ejecuta DELETE sin INSERT de reemplazo"`, vía `text.match_regex` sobre `e_tct.status.errorResult.message`) y omite la fecha sin abortar el resto del rango; cualquier otro error se relanza igual que antes (checkpoint + mail de error). **No verificado con una ejecución real todavía** — validar en la próxima corrida contra una fecha sin archivo.
  - `spec.yaml`: nueva RN-IBK-018, `etapas.integridad`/`etapas.data_quality` pasan a `false` (con nota), componentes[] y restricciones[] actualizados
  - Verificado: YAML del workflow parsea OK, ninguna expresión supera 400 bytes (máx 265), sin patrón `": "` prohibido
  - ⚠️ **Riesgo que queda obsoleto con este cambio** (ya no aplica — se deja el historial): el sub-workflow `RegisterIntegrityResults` posteaba `task_code` del SP de integridad al endpoint `.../integrity-execution/creation`; ese SP nunca quedó matriculado como task por sí mismo, riesgo de FK. Al eliminarse `RegisterIntegrityResults` del Workflow (este cambio), el riesgo deja de aplicar
- [x] **2 BUGS reales encontrados con datos reales de dev (2026-08-26), llevaron al rediseño RN-IBK-019:**
  1. **Fan-out del cruce por party_id + itc_company_id (RN-IBK-003):** el usuario reportó que su propia validación (`JOIN` por `party_id` contra `iden_itc_party`/`iden_itc_party_prd`) daba 6,773,118 filas, contra 3,979,124 insertadas por el SP. Verificado con BigQuery real (proyecto `itc-data-governance-01` para las consultas, cross-project contra `dev-intercorp-data-storage`): el archivo de Interbank siempre trae `itc_company_id='000'`, pero ~2.78M de esas personas TAMBIÉN tienen un registro en `iden_itc_party_prd` bajo `itc_company_id='1000'` (mismo `party_id`, ambos códigos son Interbank) — el JOIN de la SP exigía que coincidiera también el `itc_company_id`, así que excluía correctamente ese segundo registro. El usuario decidió que NO se debía excluir: cada coincidencia de iden_party debe generar su propia fila.
  2. **Colisión entre folder_dates con el mismo MAX(consent_date):** al preguntar por qué el archivo `2026-08-13` "no se insertaba", se encontró que `t_consent_transaction_20260813_external` y `t_consent_transaction_20260814_external` (dos archivos DISTINTOS) tenían el mismo `MAX(consent_date) = 2026-08-13`. Como el SP filtraba por ese máximo y hacía `DELETE+INSERT` por `(itc_company_id, consent_date)`, al procesar el 14 (después del 13, orden ascendente del loop) su `DELETE` borraba las ~3.9M filas que acababa de insertar el 13, dejando solo las 4,280 filas del archivo del 14 con `record_source` del 14 apuntando a `consent_date=2026-08-13`. Pérdida de datos real, confirmada en `t_consent_transaction`.
  - **Fix — rediseño completo (RN-IBK-019), 4 cambios pedidos por el usuario:**
    1. Nueva tabla `t_consent_transaction_raw` (DDL nuevo) — cada archivo del rango se vuelca TAL CUAL (sin cruzar con iden_party, sin colapsar por cliente), idempotente por `folder_date` (DELETE+INSERT si se reprocesa la misma fecha, fechas distintas se acumulan). `sp_t_consent_transaction_ibk.sql` se redujo a SOLO esto (perdió los parámetros de iden_party y `p_carga_historica`, que dejó de tener sentido).
    2. Solo se centraliza el archivo de la ÚLTIMA fecha que sí tuvo archivo (`var_ultima_fecha_cargada`, distinto de `process_date_fin` si las últimas fechas del rango no tuvieran archivo) — nueva SP `sp_centralizar_t_consent_transaction_ibk.sql`, llamada UNA vez por ejecución (no por fecha), después de `iterar_fechas`. Esto resuelve de raíz el bug #2: nunca compiten dos archivos por la misma tabla en la misma corrida.
    3. Cruce con iden_party SOLO por `party_id` (ya no también `itc_company_id`) — resuelve el bug #1, "no excluir nada". El `itc_company_id`/`id` del output ahora es el de CADA registro de iden_party que matchea, no el del archivo.
    4. `DELETE` por `itc_company_id` (no por fecha, no `TRUNCATE`) en `t_consent_transaction` y en `ba_customer_consent_group_ibk.sql` (que antes usaba `TRUNCATE`, RN-IBK-017) — ambos reemplazan TODO lo de Interbank con el resultado fresco, sin tocar otras empresas.
  - Como consecuencia, `RN-IBK-012` (filtro por `MAX(consent_date)`) y `RN-IBK-015` (carga histórica) quedaron obsoletas — ya no hace falta acotar por fecha porque cada corrida centraliza un solo archivo completo.
  - Tests: `test_sp_t_consent_transaction_ibk.sql` reescrito (ahora solo prueba la carga a raw); nuevo `test_sp_centralizar_t_consent_transaction_ibk.sql` (100% automatizado, sin dependencia de GCS — fixtures de raw + iden_party, incluyendo el caso de doble registro de empresa); `test_sp_ba_customer_consent_group_ibk.sql` actualizado (comentarios, DELETE por empresa en vez de TRUNCATE)
  - `deploy_dev.json`/`deploy_prd.json`: agregado el DDL de `t_consent_transaction_raw.sql` y la SP `sp_centralizar_t_consent_transaction_ibk.sql`; `env_dev.json`/`env_prd.json`: nueva variable `table_t_consent_transaction_raw`
  - `spec.yaml`: nueva RN-IBK-019, RN-IBK-003/005/012/015/013/017 marcadas como superadas/actualizadas (se deja el texto original por historial), componentes[] y restricciones[] actualizados
  - Verificado: YAML del workflow parsea OK, ninguna expresión supera 400 bytes (máx 265), sin patrón `": "` prohibido, comillas triples balanceadas en los 3 SP de t_consent_transaction y en el de ba_customer_consent_group
- [x] **ROLLBACK parcial pedido por el usuario (2026-08-27), RN-IBK-020 — vuelve el historial completo:** el usuario pidió revertir RN-IBK-019 en 2 de sus 4 puntos. Lógica pedida: "el primer archivo se inserta como histórico, luego sigue DELETE+INSERT por itc_company_id y consent_date en t_consent_transaction, ahí sí pueden venir duplicados (ej. un id otorgado → revocado → otorgado, t_consent_transaction mantiene esa trazabilidad) — pero primero debe haber un histórico, luego ya solo delete/insert. La ba_customer_consent_group sí se mantiene el DELETE por empresa + INSERT, y en caso de duplicados de id solo se queda con el último del archivo. Mantener también el cruce de iden_party que trae los 6MM (no excluir nada)".
  - **Eliminado del repo** (confirmado con el usuario — vivieron menos de un día): `t_consent_transaction_raw.sql` (DDL), `sp_centralizar_t_consent_transaction_ibk.sql` y su test, y sus entradas en `deploy_dev.json`/`deploy_prd.json`/`env_dev.json`/`env_prd.json` (`table_t_consent_transaction_raw`).
  - **`sp_t_consent_transaction_ibk.sql` vuelve a hacer todo directo por cada fecha del rango** (tabla externa + cruce + DELETE/INSERT en t_consent_transaction): se restauran `p_carga_historica` (RN-IBK-015, primer archivo del rango trae todo su historial si t_consent_transaction no tiene filas de IBK) y el filtro por `v_max_consent_date` (RN-IBK-012) para el resto de fechas — ambos con su comportamiento original de antes de RN-IBK-019. El cruce con iden_party sigue siendo SOLO por `party_id` (RN-IBK-019, no se revierte) — cada coincidencia de iden_party genera su propia fila, con el `itc_company_id`/`id` de ESE registro. **Sin QUALIFY**: ya no colapsa por cliente (RN-IBK-011 revertida) — el `DELETE+INSERT` es por `(itc_company_id, consent_date)`, así que un mismo `id` puede tener varias filas si aparece en fechas/archivos distintos (ej. otorgado → revocado → otorgado), preservando la trazabilidad completa.
  - **`sp_ba_customer_consent_group_ibk.sql` recupera su PROPIO `QUALIFY`** de último evento por cliente (como antes de RN-IBK-011, 2026-08-25) — con un cuidado importante documentado en la cabecera: el `ROW_NUMBER()` se calcula sobre TODOS los eventos `CP_2` del cliente (cualquier `consent_type`), y RECIÉN sobre la fila con `rn=1` se exige `consent_type = 'otorgado'`. Filtrar `consent_type` ANTES del `ROW_NUMBER()` sería un bug: se podría quedar con un `otorgado` viejo aunque el cliente haya revocado después. El DELETE por empresa (no `TRUNCATE`) y la llamada única al final del loop (RN-IBK-017) NO se revierten.
  - Tests: `test_sp_t_consent_transaction_ibk.sql` revertido a probar el flujo completo (con iden_party, `p_carga_historica`; T5 ahora documenta que el historial completo debe preservarse, ya no que colapse a 1 fila por id). `test_sp_ba_customer_consent_group_ibk.sql`: nuevos fixtures PARTY-006 (2 eventos, último `rechazado` → debe excluirse aunque tuvo un `otorgado` antes) y PARTY-007 (3 eventos, último `otorgado` → debe incluirse con los datos de ESE último evento) — nuevos T6/T7, T0/T2 pasan a esperar 4 filas.
  - `spec.yaml`: nueva RN-IBK-020; RN-IBK-005/012/015 vuelven a estar vigentes (ya no "superadas"); RN-IBK-011 pasa a superada; RN-IBK-013/006/017/019 actualizadas para reflejar el estado vigente; DDL de `t_consent_transaction.sql` (comentarios) revertido a "historial completo"; componentes[] y restricciones[] actualizados
  - Verificado: YAML del workflow parsea OK, ninguna expresión supera 400 bytes (máx 265), sin patrón `": "` prohibido, comillas triples balanceadas en ambos SP
- [x] **Validaciones con datos reales (2026-08-27), tras el rollback RN-IBK-020, llevaron a RN-IBK-021:**
  1. **Validación 1 (confirmada, sin cambios necesarios):** "todo lo que esté en `t_consent_transaction` como `otorgado`, quedándome con el último `id` en caso de duplicados, debe ser igual a `ba_customer_consent_group`" — corrida vía `itc-data-governance-01` (billing) contra `dev-intercorp-data-storage`: **coincide exactamente**, 6,763,626 filas de cada lado, 0 `id` de diferencia en ningún sentido (`EXCEPT DISTINCT` en ambas direcciones). El `QUALIFY` de `sp_ba_customer_consent_group_ibk.sql` funciona como se diseñó.
  2. **Validación 2 (reveló 2 problemas reales):** el usuario esperaba que "el archivo de la última fecha (`20260817`), tomado completo y filtrado a `otorgado`+`CP_2`, sea igual a `ba_customer_consent_group`". Al comparar: 6,772,113 (archivo 17 completo, con el mismo cruce/último-evento que usa la SP) vs 6,763,626 (`bccg` real) — **NO coinciden exactamente** (16,022 de diferencia en un sentido, 7,535 en el otro). Causa: el pipeline NO carga el archivo completo en cada fecha — solo la porción cuyo `consent_date` coincide con el `MAX(consent_date)` de ESE archivo (RN-IBK-012); para el archivo del 17 eso fueron solo 2,772 filas de las 6.8M — el resto de `bccg` viene acumulado de las 6 fechas anteriores del reproceso, no del archivo del 17. **Pendiente pedido por el usuario, no implementado aún**: quitar el filtro `MAX(consent_date)` para que cada fecha cargue su archivo completo (así el último archivo sí sería equivalente a `bccg`) — se decidió posponerlo ("por ahora solo el de la iden_party").
  3. **2 fixes SÍ pedidos y aplicados (RN-IBK-021), ambos en `sp_t_consent_transaction_ibk.sql`:**
     - **Filtro de empresa después del cruce por `party_id`**: el cruce sigue siendo solo por `party_id` (RN-IBK-019, sin excluir nada), pero ahora se agrega `AND itc_company_id IN ('000','1000')` al `WHERE` del dedup de `iden_party` — antes no existía ningún filtro sobre el `itc_company_id` de `iden_party` (solo se filtraba el del archivo, siempre `'000'`), así que una persona con registro en OTRA empresa del grupo (Interseguro, Interfondos, etc.) podía colarse en `t_consent_transaction` con ese otro código.
     - **`DELETE` por `(itc_company_id, record_source)` en vez de `(itc_company_id, consent_date)`**: corrige de raíz la colisión real confirmada el día anterior entre `t_consent_transaction_20260813_external` y `..._20260814_external` (mismo `MAX(consent_date) = 2026-08-13`) — con el `DELETE` anterior, el segundo archivo borraba todo lo insertado por el primero para esa fecha. Ahora el `DELETE` usa `record_source` (que embebe el nombre del archivo, determinístico por `folder_date`, vía `t.record_source = 'LPDP_IBK_' || s.source_file_name`) — reprocesar la MISMA fecha sigue reemplazando su propio lote (idempotente), pero un archivo nuevo nunca toca las filas de OTRO archivo, aunque compartan `consent_date`. Costo aceptado: si dos archivos distintos traen genuinamente al mismo cliente con el mismo `consent_date`, ahora pueden convivir 2 filas en vez de que el segundo reemplace al primero.
  - Tests: `test_sp_t_consent_transaction_ibk.sql` actualizado (T5 menciona `record_source` en vez de `consent_date`; nuevo T8 documentando cómo probar manualmente que 2 archivos con el mismo `MAX(consent_date)` ya no se pisan).
  - `spec.yaml`: nueva RN-IBK-021; RN-IBK-005 actualizada (ya no es por `consent_date`); componentes[] y restricciones[] actualizados.
  - Verificado: comillas triples balanceadas en `sp_t_consent_transaction_ibk.sql` (44, par).
  - Pendiente aplicar y simular con `{"process_date_init":"2026-08-07","process_date_fin":"2026-08-17"}` antes de desplegar (pedido explícito del usuario) — ver siguiente entrada.
- [x] **Simulación completa del rango 2026-08-07/17 con los 2 fixes de RN-IBK-021 (2026-08-27), contra datos reales de dev (proyecto `itc-data-governance-01`, tabla de prueba eliminada al terminar):**
  - Filtro de empresa: confirmado que NO cambia nada hoy — todos los `party_id` de estos archivos solo tienen registro en `iden_party` bajo `000`/`1000`. Es una red de seguridad para el futuro.
  - Fix de colisión: confirmado con los archivos reales del 13 y 14 — el archivo del 13 aporta 2 filas con `consent_date=2026-08-13`; el `DELETE` nuevo (por `record_source`) al procesar el 14 afectó **0 filas** (antes habría borrado esas 2). Ambos archivos coexisten correctamente: 2 + 5,135 = 5,137 filas para esa fecha, sin duplicados.
  - **Corrección a una estimación anterior**: la "pérdida de ~3.9M filas" descrita en el bug original de RN-IBK-019 estaba mal en la magnitud — el archivo del 13 en realidad solo aporta 2 filas con esa fecha (el filtro `MAX(consent_date)`, RN-IBK-012, es extremadamente restrictivo: de 6.8M filas por archivo, dejaba pasar entre 1 y 5 mil según el archivo, no millones).
  - Simulación completa (7 fechas con archivo): total `t_consent_transaction` simulado = 18,381 filas; `bccg` derivado (último evento + otorgado + CP_2) = 18,105.
  - **Hallazgo importante, no resuelto — pendiente decisión del usuario**: con el filtro `MAX(consent_date)` vigente, un cliente cuyo evento en un archivo tiene una fecha distinta a la fecha máxima de ESE archivo nunca se carga desde ahí — y si nunca coincide con el máximo de NINGÚN archivo procesado, no entra jamás a `t_consent_transaction`/`bccg`, ni siquiera vía la carga histórica (que solo cubre el contenido del PRIMER archivo del rango). Confirmado con un caso real: un `party_id` con `consent_date=2026-04-01` en el archivo del 17-ago no existe en absoluto en el archivo del 07-ago (el de la carga histórica), así que nunca pudo capturarse. El usuario decidió dejar esto pendiente por ahora (no quitar el filtro `MAX(consent_date)` todavía).
- [x] **2 validaciones de negocio pedidas por el usuario (2026-08-27), ambas confirmadas con datos reales:**
  1. "Todo lo que esté en `t_consent_transaction` como `otorgado` (último evento por `id`) debe ser igual a `bccg`" — **coincide exactamente**: 6,763,626 filas de cada lado, 0 `id` de diferencia en cualquier dirección.
  2. "El archivo de la última fecha, completo y filtrado a `otorgado`+`CP_2`, debe ser igual a `bccg`" — **NO coincide** (6,772,113 vs 6,763,626, ~23.5K de diferencia) — explicado por el filtro `MAX(consent_date)` (ver hallazgo de arriba): cada fecha solo aporta su propia porción filtrada, no el archivo completo.
- [x] **Deploy a producción — preparación (2026-08-27):**
  - El usuario completó los `pendiente_definir` de `env_prd.json` con los valores reales de producción (`project_operation`/`billing`: `silent-matter-270218`; `project_analytics`/`t_consent_transaction`/`ba_customer_consent_group`/`consentimiento_ibk_archivo`: `intercorp-data-storage-pv`; `iden_party`: `prd-itc-data-sensitive.master_pii.iden_itc_party` — nombre de tabla DISTINTO al de dev, `iden_itc_party` vs `iden_itc_party_prd`). `service_account_app` queda sin definir a propósito (este módulo no tiene Cloud Function/Cloud Run, nunca se referencia).
  - Verificado con datos reales de producción (antes de desplegar) que los `DELETE` de ambos SP (`sp_t_consent_transaction_ibk.sql` por `itc_company_id`+`record_source`; `sp_ba_customer_consent_group_ibk.sql` por `itc_company_id`) NUNCA tocan otras empresas — confirmado que `t_consent_transaction`/`ba_customer_consent_group` en prod ya tienen millones de filas de CINEPLANET (013), INTERSEGURO (002), INTERFONDOS (001) e INTELIGO SAB (018), ninguna con código `000`/`1000`.
  - Creada la rama `prd` (no existía) desde `dev`, con el mismo contenido — para desplegar producción desde ahí.
  - **BUG real encontrado en el primer intento de deploy en prd (RN-IBK-022)**: `sp_t_consent_transaction_ibk.sql` falló con `Dataset prd-itc-data-sensitive:master_stage was not found in location US` — la ruta de la tabla de stage usaba `${project_iden_party}`, asumiendo (por una coincidencia que solo se daba en dev) que el dataset de stage vive bajo el mismo proyecto que `iden_party`. En prod, `project_iden_party` es `prd-itc-data-sensitive` (un proyecto de datos sensibles/PII) que NO hospeda `master_stage` — verificado que ese dataset sí existe bajo `intercorp-data-storage-pv` (confirmado por el usuario) y también bajo `silent-matter-270218`, pero no bajo el proyecto de iden_party.
    - Fix: nueva variable de deploy `project_stage` en `env_dev.json` (`dev-intercorp-data-operation`, preserva el comportamiento ya validado) y `env_prd.json` (`intercorp-data-storage-pv`). `sp_t_consent_transaction_ibk.sql` y `test_sp_t_consent_transaction_ibk.sql` (T7) actualizados para usar `${project_stage}` en vez de `${project_iden_party}`.
    - `spec.yaml`: nueva RN-IBK-022, componente y restricciones actualizados.
    - Verificado: comillas triples balanceadas (44, par), JSON válido en ambos `env_*.json`.
    - Subido a `dev` (`7c038fa`) y mergeado (fast-forward) a `prd`.
  - **2do BUG real, en el reintento de deploy en prd (RN-IBK-023)**: `sp_t_consent_transaction_ibk.sql` falló con `Query column 18 has type DATETIME which cannot be inserted into column load_date, which has type TIMESTAMP`. Verificado con `INFORMATION_SCHEMA.COLUMNS` real: `t_consent_transaction.load_date` es `TIMESTAMP` en prod (la tabla ya existía con datos de otras empresas antes de este módulo, con el tipo del contrato original) pero `DATETIME` en dev (bug ya documentado 2026-08-21, cuando el SP se ajustó a `CURRENT_DATETIME()`) — el tipo real difiere POR AMBIENTE.
    - Fix: el SP ya no asume ningún tipo fijo — consulta `INFORMATION_SCHEMA.COLUMNS` en runtime para el tipo real de `load_date` y arma el literal correcto (`CURRENT_TIMESTAMP()` o `CURRENT_DATETIME('America/Lima')`), funciona en ambos ambientes sin importar el tipo real de la tabla.
    - Verificado con datos reales que `ba_customer_consent_group` NO tiene este problema — `load_date` es `DATETIME` en ambos ambientes (dev y prod), no necesita el mismo fix.
    - `spec.yaml`: nueva RN-IBK-023, componente y restricciones actualizados.
    - Verificado: comillas triples balanceadas (54, par).
    - Subido a `dev` (`d8236ad`) y mergeado (fast-forward) a `prd`.
- [x] **Cambio pedido por el usuario (2026-08-28), RN-IBK-024 — ba_customer_consent_group cambia de dataset:** de `master_party` a `ba_consent`, en ambos ambientes. Cambio puramente de configuración (`dataset_ba_customer_consent_group` en `env_dev.json`/`env_prd.json`) — confirmado que cero código necesita cambiar: DDL, Workflow y `sp_ba_customer_consent_group_ibk.sql` ya referencian el dataset solo vía esa variable, sin ningún nombre hardcodeado. `t_consent_transaction` NO se mueve, sigue en `master_party`.
  - ⚠️ **Riesgo detectado antes de que rompiera el deploy**: el dataset `ba_consent` **no existe todavía** en ninguno de los 2 proyectos — verificado con `INFORMATION_SCHEMA.SCHEMATA` en `dev-intercorp-data-storage` e `intercorp-data-storage-pv` (ambos sin resultados). `CREATE TABLE IF NOT EXISTS` crea la tabla pero no el dataset — si no se aprovisiona antes del deploy (InfraOps o creación manual), va a fallar con "Dataset ba_consent was not found", el mismo tipo de error que RN-IBK-022. **Avisado al usuario, pendiente de confirmación/aprovisionamiento antes de desplegar.**
  - `spec.yaml`: nueva RN-IBK-024, restricciones actualizados (marcada PENDIENTE, no RESUELTO, hasta confirmar que el dataset existe).
  - Verificado: JSON válido en ambos `env_*.json`.
  - **Confirmado por el usuario (2026-08-28): el dataset `ba_consent` ya fue creado** — RN-IBK-024 pasa a RESUELTO.
  - **Pedido del usuario, este deploy a prod solo debe tocar los SP**: `deploy_prd.json` se reduce a únicamente la sección `bigquery_sp` — se quitan `bigquery_ddl`, `workflow`, `cloud_scheduler` y `monitoring_register` de este archivo (no se van a desplegar en esta corrida). `deploy_dev.json` NO se toca, sigue desplegando todas las secciones en dev.
  - **Ajuste pedido por el usuario**: como `ba_consent` es un dataset nuevo, la tabla `ba_customer_consent_group` todavía no existe ahí — se vuelve a agregar `bigquery_ddl` a `deploy_prd.json`, pero SOLO con `ba_customer_consent_group.sql` (no las demás DDL del módulo, que ya existen en `master_party`). ⚠️ Nota no confirmada con el usuario: la tabla nueva se crea solo con el DDL base — NO se incluyó `alter_ba_customer_consent_group_20260819_001.sql` (agrega `dq_flag_ind`/`dq_control_msg`/`dq_config_id`, columnas nunca pobladas porque DQ está deshabilitado, RN-IBK-018) — si se quiere que el schema quede idéntico al de la tabla vieja en `master_party`, faltaría agregar también ese alter.
  - **3er BUG real, reportado por el usuario tras el deploy (2026-08-28): "no se inserta data en `ba_consent.ba_customer_consent_group`".** Diagnóstico con datos reales de producción: `t_consent_transaction` sí tiene 6,795,764 filas de Interbank (el SP1 funciona bien); `ba_consent.ba_customer_consent_group` existe pero tiene 0 filas; el conteo esperado calculado a mano con la misma lógica `QUALIFY` del SP da exactamente 6,795,102 filas. Se confirmó con `INFORMATION_SCHEMA.JOBS_BY_PROJECT` (proyecto `silent-matter-270218`) el SQL real del último `CALL` a `sp_ba_customer_consent_group_ibk` — seguía pasando `'master_party'` como dataset, NO `'ba_consent'`. Causa raíz: como el deploy anterior excluyó a propósito la sección `workflow` (punto de arriba, "este deploy a prod solo debe tocar los SP"), el Workflow compilado en Cloud Workflows nunca se volvió a desplegar — y Cloud Workflows resuelve los placeholders `${...}` en tiempo de DEPLOY (quedan "horneados" como texto literal), no en tiempo de ejecución. Cambiar la variable en `env_prd.json` no tiene ningún efecto sobre un Workflow ya desplegado hasta que ese Workflow se vuelva a desplegar. Se verificó además que exactamente 6,795,102 filas (el conteo esperado) quedaron insertadas en la ubicación vieja, `intercorp-data-storage-pv.master_party.ba_customer_consent_group` — es decir, el SP nunca tuvo un bug de lógica, solo se lo siguió llamando con el dataset viejo.
    - Fix: se agrega `workflow` de vuelta a `deploy_prd.json` (`/pipeline/workflow/master_party/t_consent_transaction/wf-ibk-consentimiento.yaml`) para que se recompile con `dataset_ba_customer_consent_group = ba_consent` en el próximo deploy — confirmado por el usuario ("Sí, agrégalo (Recomendado)").
    - **Decisión explícita del usuario sobre las 6,795,102 filas ya insertadas en `master_party.ba_customer_consent_group`: quedan ahí, no se tocan** ("Déjalas ahí, no las toques") — no se borran ni se migran a `ba_consent` en esta pasada.
    - `spec.yaml`: nota agregada a RN-IBK-024 documentando este hallazgo.
    - Pendiente: volver a correr el trigger de deploy en `prd` (ahora con DDL+SP+workflow) para que el Workflow quede recompilado con el dataset correcto.
- [x] **Corrección de modelo de carga pedida por el usuario (2026-08-28), RN-IBK-025 — resuelve el pendiente que se había dejado abierto el 2026-08-27 (ver arriba, "el usuario decidió posponerlo").** El usuario confirmó: el archivo de origen de Interbank es un extracto COMPLETO/acumulado, no incremental — "el insert, para t_consent_transaction y ba_customer_consent_group, será solo del último archivo, una carga total". Cambios:
  - **Solo se procesa el último archivo**: normal/manual ya eran un solo archivo (sin cambio). En reproceso por rango, confirmado explícitamente por el usuario (AskUserQuestion: "Eliminar el loop (Recomendado)") — el Workflow YA NO itera fecha por fecha: `modo_reproceso` asigna `var_process_dates = [process_date_fin]` directamente, sin `GENERATE_DATE_ARRAY` ni el loop `iterar_fechas` (se eliminaron ambos, junto con `extraer_fechas_reproceso`/`construir_lista_fechas`).
  - **Carga total**: se elimina la sección 4b de `sp_t_consent_transaction_ibk.sql` (cálculo de `v_max_consent_date`/`v_date_filter_clause`, RN-IBK-012) — el archivo se carga COMPLETO siempre, sin filtro de `consent_date`. Con esto, `p_carga_historica` (RN-IBK-015) deja de tener sentido — se elimina el parámetro de la firma del SP y toda la lógica asociada en el Workflow (`inicializar_carga_historica`, `verificar_datos_ibk_existentes`, `build_sql_check_ibk`, `ejecutar_check_ibk`, `evaluar_carga_historica`, `log_carga_historica`, `evaluar_carga_historica_fecha`).
  - **DELETE por `itc_company_id`**: el `DELETE` de la carga final en `t_consent_transaction` vuelve a ser `WHERE itc_company_id IN ('000','1000')` (sin la subquery `EXISTS` contra `record_source` de RN-IBK-021) — cada corrida es un espejo completo de Interbank. `sp_ba_customer_consent_group_ibk.sql` **no cambió** — ya hacía exactamente ese patrón (DELETE por `itc_company_id` + INSERT con su propio `QUALIFY` de último evento y el filtro `CP_2`/`otorgado`, RN-IBK-020).
  - Riesgo aceptado (documentado en `spec.yaml`): a diferencia de RN-IBK-021 (protegía contra colisión entre archivos), ya no hay ninguna protección si un archivo de una corrida no trae la historia completa vigente — el `DELETE` por empresa borra igual todo lo anterior. Se asume por diseño que cada archivo procesado siempre es el extracto completo.
  - Tests: `test_sp_t_consent_transaction_ibk.sql` — quitado `p_carga_historica` de los `CALL` (T1, T3); T5 reescrito (ya no habla de reprocesar 2 fechas distintas, sino de que un mismo archivo puede traer un `id` repetido en más de un `consent_date`); T6 reescrito y automatizado (ya no depende de un flag, verifica `COUNT(DISTINCT consent_date) >= 1` tras la carga completa); T8 reescrito — ya NO prueba que 2 archivos coexistan (ese escenario ya no aplica), ahora documenta que el segundo archivo REEMPLAZA por completo al primero (comportamiento esperado bajo RN-IBK-025, no un bug).
  - `spec.yaml`: nueva RN-IBK-025; RN-IBK-012/RN-IBK-015 marcadas SUPERADAS (se deja el texto de la versión RESTAURADA por historial); RN-IBK-021 marcada con nota de que su punto (2) quedó superado; componentes[] (SP y Workflow) y restricciones[] actualizados.
  - Verificado: comillas triples balanceadas en `sp_t_consent_transaction_ibk.sql` (46, par), YAML del workflow parsea OK, YAML del spec parsea OK.
  - Pendiente: aplicar el mismo commit/push/sync a `prd` que el resto de cambios de esta sesión, y desplegar.
- [x] **BUG real confirmado (2026-08-21), primera ejecución del workflow:** falló con `exception: URL not reachable ... routine UpdateProcessStatus ... in step update_process_failed`. Diagnóstico: `UpdateProcessStatus`/`CreateExecution`/`UpdateExecution` NO tenían try/except propio (a diferencia de `RegisterIntegrityResults`, que sí atrapa su error y loguea WARNING) — un hiccup de conectividad a la API de metadata (verificado externamente con curl: el servicio SÍ responde, HTTP 200/405 — probablemente cold-start/timeout puntual de Cloud Run, no una URL mala) se propagó sin control y **tapó el error real** que originalmente disparó el `except` (nunca lo vimos). Más grave: `UpdateExecution` se invoca también dentro del `except` de `TrackedBigQueryJobWithResults` — si falla ahí, pierde el error real de BigQuery antes de poder hacer `raise_error`. Fix: los 3 sub-workflows ahora atrapan su propio error y loguean WARNING (mismo patrón que `RegisterIntegrityResults`), nunca rompen el pipeline ni tapan el error original. **Pendiente:** volver a correr el workflow para ver el error real de negocio (probablemente "0 filas en la fuente principal" si aún no se subió el archivo de prueba a `gs://dev-demo-raw-sales-xrt9/...`)

### ORCHESTRATION
- [x] Workflow: `wf-ibk-consentimiento.yaml` — normal (fecha de sistema, NO "ayer" — desviación intencional del default del framework, ver RN-IBK-009)/manual (`process_date`)/reproceso (`process_date_init`+`process_date_fin`, RN-IBK-025: ya NO itera el rango — solo procesa `process_date_fin`, el loop `GENERATE_DATE_ARRAY`+`for` se eliminó)
- [x] Cloud Scheduler: `cs-ibk-consentimiento.yaml` — diario `0 10 * * *` America/Lima, dispara modo normal (`argument: {"process_date": ""}`)
- [x] ~~⚠️ Validar en TESTING el parseo de `bqRangoFechas.rows`...~~ — **obsoleto (RN-IBK-025, 2026-08-28)**: ese código (`GENERATE_DATE_ARRAY`, `construir_lista_fechas`) se eliminó del Workflow, ya no hay nada que validar ahí.
- [x] Confirmado: si una fecha falla a mitad de un reproceso, el workflow se detiene ahí (comportamiento esperado, no todo-o-nada). Se agregó `var_ultima_fecha_intentada` como checkpoint — el `except` lo imprime en logs (`severity: ERROR`) y en el mail de error, con la sugerencia exacta de con qué `process_date_init`/`process_date` reanudar
- [x] Correos reemplazados por `lsullonm@intercorp.com.pe` **solo para pruebas** — ⚠️ pendiente reemplazar por los destinatarios reales antes de producción (queda marcado inline en el YAML)

### TESTING
- [x] `fac-data-stage-testing` ejecutado (2026-08-19) — **⛔ BLOCKED**: sin acceso MCP a `dev-intercorp-data-storage`/`dev-intercorp-data-operation`, y ningún SP del módulo existe en `dev-itc-customer-services.demo_migracion` → ningún artefacto está desplegado en dev todavía. Reporte: `docs/reports/testing-2026-08-19.md`
- [ ] Ejecutar trigger InfraOps (SAs + permisos IAM) y trigger Dataops dev (DDL/SPs/Workflow/Scheduler), confirmar SUCCESS en ambos
- [ ] Re-ejecutar `fac-data-stage-testing` una vez desplegado
- [ ] Ejecutar DDL en dev
- [ ] Validar conteos vs archivo fuente
- [ ] Validar las 5 reglas DQ (una vez activada la etapa)
- [ ] Probar los 3 escenarios: normal, manual (`process_date` único), reproceso (rango de fechas)

### DATAOPS
- [x] Backup de `deploy_dev.json`/`deploy_prd.json`/`env_dev.json`/`env_prd.json` en `deploy/backup/*_20260819172956.json` antes de regenerar
- [x] `deploy_dev.json`/`deploy_prd.json` regenerados desde cero a partir de `componentes[]` — 2 `bigquery_ddl`, 5 `bigquery_sp`, `bigquery_dml` (solo dev, 2 configs DQ), `workflow`, `cloud_scheduler`, `monitoring_register`
- [x] Confirmado: el componente `ddl` de referencia `ext_t_consent_transaction_external.sql` quedó excluido de `bigquery_ddl` (como se venía marcando desde PHYSICAL_DESIGN)
- [x] Cobertura de variables validada: todo `${...}` genuino de Dataops usado en `data/bigquery/master_party/`, `data/monitoring/`, `data/integrity/` y `pipeline/` ya está en `env_dev.json`. No hizo falta agregar ninguna variable nueva
- [ ] Completar `env_prd.json`: `project_operation`, `project_analytics`, `project_billing`, `dataset_sp`, `dataset_stage`, `dataset_dq`, `service_account_job`, `service_account_app`, `project_consentimiento_ibk_archivo`, `dataset_consentimiento_ibk_archivo`, `project_iden_party`, `dataset_iden_party`, `project_t_consent_transaction`, `project_ba_customer_consent_group` siguen en `pendiente_definir`
- [ ] Variables huérfanas detectadas (no usadas por ningún artefacto, no bloqueante): `project_analytics`, `project_billing` en ambos env — el módulo usa el patrón de 3-variables-por-tabla en su lugar. `table_consentimiento_ibk_archivo` tampoco se referencia directamente (el nombre real se arma dinámico en el SP) — se dejó solo como documentación

### SECURITY
- [x] `fac-data-stage-reality-check` (2026-08-19): confirmado que `project_operation`/`project_analytics`/`dataset_sp`/`dataset_stage`/`dataset_dq` y `service_account_job` en `env_dev.json` son valores de un proyecto/SA operativo **compartido** (no exclusivos del módulo) — patrón esperado, no un resto de otro módulo. `seguridad.permisos` del spec ahora referencia `${service_account_job}` en vez de un literal distinto
- [ ] Solicitar/confirmar que `${service_account_job}` tenga efectivamente `dataViewer` sobre `iden_itc_party_prd` y `dataEditor` sobre `master_party` (el equipo indica que sí, pendiente de verificación formal en INFRAOPS)
- [x] **BUG real confirmado (2026-08-20), primera corrida del workflow en dev:** `Access Denied: ... does not have storage.objects.list access to the Google Cloud Storage bucket ... p-ibkbi-rdp-stg-dlk-us-suoh`. La SA `dev-demo-migra-matillion-job@dev-itc-customer-services.iam.gserviceaccount.com` (`service_account_job`) no tiene permiso de lectura sobre el bucket GCS donde IBK deposita el archivo diario — necesario porque `sp_t_consent_transaction_ibk.sql`/`sp_integridad_t_consent_transaction.sql` crean una EXTERNAL TABLE apuntando directo a `gs://p-ibkbi-rdp-stg-dlk-us-suoh/...`. **Pendiente en prd: otorgar rol `roles/storage.objectViewer` sobre el bucket `p-ibkbi-rdp-stg-dlk-us-suoh` a la SA de prd** — coordinar con el equipo dueño del bucket (Interbank) o InfraOps
- [x] **Workaround para dev (2026-08-20), decisión del usuario:** en vez de tramitar el permiso sobre el bucket real de IBK solo para probar en dev, se agregó la variable Dataops `gcs_bucket_consentimiento_ibk_archivo` (antes hardcodeada como literal en el SP, violaba REGLA 1). `env_dev.json` → `dev-demo-raw-sales-xrt9` (bucket propio del dev); `env_prd.json` → `p-ibkbi-rdp-stg-dlk-us-suoh` (el real de IBK). Actualizados: `sp_t_consent_transaction_ibk.sql`, `sp_integridad_t_consent_transaction.sql`, `ext_t_consent_transaction_external.sql` (referencia), comentario de `test_sp_t_consent_transaction_ibk.sql`. **Pendiente:** subir un `.txt.gz` de prueba a `gs://dev-demo-raw-sales-xrt9/data/m_consent/current/{folder_date}/{uuid}/T_IN_LPDP_CONSENTIMIENTO_{folder_date}.txt.gz` antes de volver a correr el workflow en dev — sin archivo ahí, el SP sigue sin encontrar filas (aunque el permiso ya no bloquee)

### DOCUMENTATION
- [ ] Actualizar glosario de `t_consent_transaction` y `ba_customer_consent_group`
- [ ] Actualizar README del repo
- [ ] Generar diagramas de arquitectura (`fac-data-diagrams`)
