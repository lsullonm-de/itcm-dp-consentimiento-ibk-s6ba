# Reporte TESTING — 2026-08-19
> **Repo:** `itcm-dp-consentimiento-ibk-s6ba` · **SPEC:** spec-ibk-20260819-001
> **Resultado:** ⛔ BLOCKED — prerequisito no cumplido

## Prerequisito — Verificar pre-condiciones

Verificación vía MCP BigQuery (proyecto de billing/query: `dev-itc-customer-services`, único con `mcp.tools.call` habilitado en esta sesión):

| Verificación | Resultado |
|---|---|
| Acceso a `dev-intercorp-data-storage` (proyecto de `t_consent_transaction`/`ba_customer_consent_group`) | ❌ `Access Denied: User does not have mcp.tools.call permission` |
| Acceso a `dev-intercorp-data-operation` (proyecto de `iden_itc_party_prd`) | No probado — ya bloqueado por el punto anterior |
| SPs del módulo en `dev-itc-customer-services.demo_migracion` (`dataset_sp`) | ❌ No existen. Tablas presentes en ese dataset: `m_product`, `m_promotion`, `t_payment`, `t_retail_transaction` — ninguna es de este módulo (`sp_t_consent_transaction_ibk`, `sp_integridad_t_consent_transaction`, `sp_ba_customer_consent_group_ibk`, `sp_dq_t_consent_transaction`, `sp_dq_ba_customer_consent_group`) |

**Conclusión:** ningún artefacto de `consentimiento-ibk` (DDL, SPs, Workflow, Scheduler) está desplegado en dev. No se ejecutó ningún trigger de Cloud Build (InfraOps ni Dataops) para este módulo durante esta sesión — todo el trabajo hasta ahora fue generación de archivos, no despliegue.

```
⛔ TESTING BLOCKED — tablas/SPs no encontrados en dev.
   Ejecutar primero:
   1. Trigger InfraOps → crear SAs y permisos IAM
      (incluye el pendiente ya anotado en TODO.md/SECURITY: permisos de
      ${service_account_job} sobre iden_itc_party_prd y master_party)
   2. Trigger Dataops (dev) → desplegar DDL, SPs, Workflow, Scheduler
   Confirmar SUCCESS en ambos triggers antes de re-ejecutar fac-data-stage-testing.
```

No se ejecutaron T1–T6 — todos dependen de que los objetos existan en dev.

---
> 📁 Generado por `fac-data-stage-testing` · 2026-08-19
> Próximo paso: ejecutar triggers InfraOps + Dataops (dev), confirmar SUCCESS, volver a `fac-data-stage-testing`
